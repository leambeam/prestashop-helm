{{- if .Values.prestashop.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "prestashop.fullname" . }}
  labels:
    {{- include "prestashop.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "prestashop.fullname" . }}
  minReplicas: {{ .Values.prestashop.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.prestashop.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.prestashop.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.prestashop.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.prestashop.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.prestashop.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
