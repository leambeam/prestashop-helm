{{- if .Values.prestashop.ingress.enabled -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "prestashop.fullname" . }}
  labels:
    {{- include "prestashop.labels" . | nindent 4 }}
  {{- with .Values.prestashop.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- with .Values.prestashop.ingress.className }}
  ingressClassName: {{ . }}
  {{- end }}
  {{- if .Values.prestashop.ingress.tls }}
  tls:
    {{- range .Values.prestashop.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.prestashop.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            {{- with .pathType }}
            pathType: {{ . }}
            {{- end }}
            backend:
              service:
                name: {{ include "prestashop.fullname" $ }}
                port:
                  number: {{ $.Values.prestashop.service.ports.https }}
          {{- end }}
    {{- end }}
{{- end }}
