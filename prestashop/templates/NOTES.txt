{{- /*
Generate the application URL for each path in `ingress.paths`.
- Uses `http` if ingress is disabled.
- Uses `https` if ingress is enabled.
*/}}

Your application URL is:

{{- range $path := .Values.prestashop.ingress.paths }}
http{{ if $.Values.prestashop.ingress.enabled }}s{{ end }}://{{ $.Values.prestashop.env.psDomain }}{{ $path.path }}
{{- end }}